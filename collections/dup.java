import java.util.*;

public class rem {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        ArrayList<Integer> list = new ArrayList<>();
        int n = Sc.nextInt();
        System.OUT.println("getting the elements fro the user:"+n);
        for(int i=0;i<n;i++){
            list.add(sc.nextInt());
        }
        linked
    }}